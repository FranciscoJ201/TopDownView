# Ultralytics Tracker Configuration
tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6 #0.6
track_buffer: 120        # Your custom memory (4 seconds)
match_thresh: 0.8 #0.8

# --- Missing Keys causing the crash ---
fuse_score: True         # Fuses detection score with tracking score (Required)
gmc_method: sparseOptFlow # Global Motion Compensation (Required)
proximity_thresh: 0.5
appearance_thresh: 0.25
with_reid: False